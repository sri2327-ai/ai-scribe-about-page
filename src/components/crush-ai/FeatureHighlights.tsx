
import React, { memo } from "react";

// This file is entirely emptied as the section is removed
// We're creating a proper memo component instead of the previous null component
export const FeatureHighlights = memo(() => null);

FeatureHighlights.displayName = 'FeatureHighlights';


import React, { memo } from "react";

// This file has been emptied as the section is removed
// We're creating a proper memo component instead of the previous null component
export const TestimonialsBenefitsSection = memo(() => null);

TestimonialsBenefitsSection.displayName = 'TestimonialsBenefitsSection';

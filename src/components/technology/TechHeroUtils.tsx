
import React from 'react';
import { Box, BoxProps } from '@mui/material';
import { Theme } from '@mui/material/styles';

export const TechHeroBox: React.FC<BoxProps> = (props) => {
  return <Box {...props} />;
};
